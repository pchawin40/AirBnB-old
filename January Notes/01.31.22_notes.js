git branch - M main

/* Transferee */
git clone link

ls

    (no need to do git init, only one person ever run git init)

git status

git add.

git commit

/* Transferor again */
Git pull(to transfer from Github)
Git push(to transfer to Github)

// Save init
Git add.
Git commit - m "message"
Git push

// Driver-Navigator:
// say add code to line "n and a half" (12 and a half) etc

// Make a function that print a string
function printString(string) {
    //your code here
}